<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Book Store</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link rel="stylesheet" href="styles/styles.css">
</head>
<body>

  <div class="container mx-auto p-4 px-24 pt-6 bg-gradient-to-r from-slate-300 to-gray-300">
    <h1 class="text-xl text-center mb-4">Book Showcase</h1>

    <% if (books.length > 0) { %>
      <div class="books-grid grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
        <% books.forEach(book => { %>
          <div class="book-details shadow-md rounded-lg p-4">
            <div class="book-cover mb-4 min-h-80">
              <img src="https://covers.openlibrary.org/b/isbn/<%= book.isbn %>-M.jpg" alt="<%= book.title %> Cover" class="w-full h-auto rounded-lg object-contain" loading="lazy">
            </div>
            <div class="book-info">
              <h2 class="text-xl mb-2"><%= book.title %></h2>
              <p class="text-md text-gray-600 mb-2">By <%= book.author %></p>
              <p class="text-md text-orange-500 font-bold mb-2">Rating: <%= book.rating %>/5</p>
              <p class="text-md text-gray-600 mb-2"> <span class="text-gray-700 font-semibold">Date Read : </span> <%= book.date_read.toLocaleString('en-US', { dateStyle: 'medium' }) %></p>
              <p class="text-md text-gray-600 mb-2 line-clamp-5 text-justify"> <span class="text-gray-700 font-semibold">Summary : </span><%= book.summary %>  </p>
              <p class="text-md text-gray-600 mb-2"><span class="text-gray-700 font-semibold">ISBN : </span> <%= book.isbn %></p>
              <button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded mt-4" onclick="window.location.href='/book/<%= book.isbn %>'">Read Full Book</button>
            </div>
          </div>
        <% }); %>
      </div>
    <% } else { %>
      <p class="text-lg text-gray-600 mb-2">No books found.</p>
    <% } %>
  </div>
</body>
</html>